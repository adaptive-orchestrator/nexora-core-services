# =============================================================================
# LLM Orchestrator Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# GEMINI API CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Single API Key (legacy)
GEMINI_API_KEY=your-gemini-api-key-here

# Multiple API Keys for Rate Limit Handling (comma-separated)
# Format: key1,key2,key3
# The service will rotate through keys when rate limits are hit
GEMINI_API_KEYS=key1,key2,key3

# Model to use (default: gemini-2.0-flash-exp)
LLM_MODEL=gemini-2.0-flash-exp
# Alternative: GEMINI_MODEL=gemini-2.0-flash-exp

# -----------------------------------------------------------------------------
# RAG (Retrieval-Augmented Generation) Configuration
# -----------------------------------------------------------------------------
# Enable/disable RAG for code search context
USE_RAG=true

# Qdrant Vector Database URL
QDRANT_URL=http://localhost:6333

# Qdrant Collection Name
QDRANT_COLLECTION=codebase_embeddings

# -----------------------------------------------------------------------------
# TEXT-TO-SQL CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Enable Text-to-SQL feature
TEXT_TO_SQL_ENABLED=false

# Database URL for SQL execution (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Enable database query logging
DB_LOGGING=false

# -----------------------------------------------------------------------------
# AUTO DEPLOYMENT CONFIGURATION
# -----------------------------------------------------------------------------
# Enable automatic Helm deployment trigger
AUTO_DEPLOY_ENABLED=false

# Default to dry-run mode (no actual deployment)
DEFAULT_DRY_RUN=true

# -----------------------------------------------------------------------------
# JWT CONFIGURATION
# -----------------------------------------------------------------------------
JWT_SECRET=your-jwt-secret
TOKEN_EXPIRE_TIME=3600

# -----------------------------------------------------------------------------
# KAFKA CONFIGURATION
# -----------------------------------------------------------------------------
KAFKA_BROKERS=localhost:9092
