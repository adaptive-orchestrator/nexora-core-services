apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: billing-db-data
  name: billing-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
