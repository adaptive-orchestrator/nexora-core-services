apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yaml -o k8s/
    kompose.version: 1.37.0 (fb0539e64)
  labels:
    io.kompose.service: order-service
  name: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: order-service
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yaml -o k8s/
        kompose.version: 1.37.0 (fb0539e64)
      labels:
        io.kompose.service: order-service
    spec:
      containers:
        - name: order-service
          image: 0xt4i/order-service:0.0.1
          ports:
            - containerPort: 3011
          envFrom:
            - configMapRef:
                name: env
            - secretRef:
                name: mysql-secret
      restartPolicy: Always

